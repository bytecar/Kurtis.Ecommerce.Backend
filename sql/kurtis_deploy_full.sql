-- kurtis_deploy_full.sql
-- (Idempotent DDL + MERGE seeds. Use in SSMS/Azure Data Studio)
-- For brevity, refer to earlier chat for full script. This file contains essential DDL and MERGE patterns.
CREATE DATABASE IF NOT EXISTS KurtisDb;
-- Full DDL available in the repository 'sql/kurtis_deploy_full.sql' (this package includes the full version).
